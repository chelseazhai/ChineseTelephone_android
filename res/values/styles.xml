<resources>

    <!-- application default theme -->
    <style name="AppTheme" parent="android:Theme.Light.NoTitleBar" />

    <!-- application activity theme -->
    <style name="AppActivityTheme" parent="android:Theme.Black.NoTitleBar.Fullscreen" />

</resources>