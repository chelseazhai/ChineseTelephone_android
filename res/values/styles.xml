<resources>

    <!-- application default theme -->
    <style name="AppTheme" parent="android:Theme.Light.NoTitleBar" />

    <!-- application launch activity theme -->
    <style name="AppLaunchActivityTheme" parent="android:Theme.Black.NoTitleBar.Fullscreen" />

</resources>