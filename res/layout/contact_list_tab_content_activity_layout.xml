<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >

        <FrameLayout
            android:id="@+id/contact_searchBtn_frameLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true" >

            <Button
                android:id="@+id/contact_search_confirm_btn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/contact_search_confirm_btn_title" />

            <Button
                android:id="@+id/contact_search_cancel_btn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/contact_search_cancel_btn_title"
                android:visibility="gone" />
        </FrameLayout>

        <EditText
            android:id="@+id/contact_search_editText"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_toLeftOf="@id/contact_searchBtn_frameLayout"
            android:hint="@string/contact_search_editText_hint"
            android:inputType="text" >

            <requestFocus />
        </EditText>
    </RelativeLayout>

    <ListView
        android:id="@+id/contactInAB_listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </ListView>

</LinearLayout>